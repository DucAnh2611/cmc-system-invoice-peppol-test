# PDF Assets

Place your invoice PDF files in this directory.

The mock data references `/pdfs/sample-invoice.pdf`, so you should add a `sample-invoice.pdf` file here for testing.

You can add more PDF files and update the mock data in `src/data/mockInvoices.ts` to reference them.
